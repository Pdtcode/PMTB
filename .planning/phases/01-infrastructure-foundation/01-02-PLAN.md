---
phase: 01-infrastructure-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pmtb/kalshi/__init__.py
  - src/pmtb/kalshi/auth.py
  - src/pmtb/kalshi/client.py
  - src/pmtb/kalshi/errors.py
  - tests/kalshi/__init__.py
  - tests/kalshi/test_auth.py
  - tests/kalshi/test_retry.py
autonomous: true
requirements:
  - INFR-01
  - INFR-06

must_haves:
  truths:
    - "RSA-PSS headers are generated with correct KALSHI-ACCESS-KEY, KALSHI-ACCESS-TIMESTAMP, and KALSHI-ACCESS-SIGNATURE fields"
    - "Every REST request uses freshly signed headers — no header caching"
    - "429 and 5xx errors trigger exponential backoff with jitter up to 5 retries"
    - "4xx client errors (except 429) are raised immediately without retry"
    - "KalshiClient wraps SDK and injects auth headers per request"
  artifacts:
    - path: "src/pmtb/kalshi/auth.py"
      provides: "RSA-PSS signing function"
      exports: ["build_kalshi_headers", "load_private_key"]
    - path: "src/pmtb/kalshi/client.py"
      provides: "KalshiClient wrapping SDK with auth"
      exports: ["KalshiClient"]
    - path: "src/pmtb/kalshi/errors.py"
      provides: "Error types and retry decorator"
      exports: ["KalshiRateLimitError", "KalshiServerError", "KalshiClientError", "kalshi_retry"]
  key_links:
    - from: "src/pmtb/kalshi/client.py"
      to: "src/pmtb/kalshi/auth.py"
      via: "build_kalshi_headers called per request"
      pattern: "build_kalshi_headers"
    - from: "src/pmtb/kalshi/client.py"
      to: "src/pmtb/config.py"
      via: "Settings injected at construction"
      pattern: "settings.*kalshi_api_key_id"
    - from: "src/pmtb/kalshi/errors.py"
      to: "tenacity"
      via: "kalshi_retry decorator with wait_exponential_jitter"
      pattern: "wait_exponential_jitter"
---

<objective>
Kalshi REST API client with RSA-PSS authentication and retry logic.

Purpose: All Kalshi interactions go through KalshiClient. The scanner (Phase 2), executor (Phase 6), and reconciler (INFR-07 in Plan 04) all depend on this client.
Output: Working KalshiClient with auth signing, error categorization, and exponential backoff retry.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RSA-PSS auth signing and error categorization</name>
  <files>
    src/pmtb/kalshi/__init__.py, src/pmtb/kalshi/auth.py, src/pmtb/kalshi/errors.py,
    tests/kalshi/__init__.py, tests/kalshi/test_auth.py, tests/kalshi/test_retry.py
  </files>
  <behavior>
    - Test 1: build_kalshi_headers returns dict with KALSHI-ACCESS-KEY, KALSHI-ACCESS-TIMESTAMP, KALSHI-ACCESS-SIGNATURE keys
    - Test 2: Signature is base64-encoded and non-empty
    - Test 3: Timestamp is current time in milliseconds (within 2 seconds)
    - Test 4: Path with query parameters — query string is stripped before signing
    - Test 5: load_private_key loads a PEM RSA private key from file path
    - Test 6: kalshi_retry retries on KalshiRateLimitError up to 5 times
    - Test 7: kalshi_retry retries on KalshiServerError up to 5 times
    - Test 8: kalshi_retry does NOT retry on KalshiClientError (raises immediately)
    - Test 9: kalshi_retry uses exponential backoff with jitter (wait_exponential_jitter)
  </behavior>
  <action>
    1. Create `src/pmtb/kalshi/__init__.py` (empty).
    2. Create `src/pmtb/kalshi/auth.py`:
       - `load_private_key(path: str)` — reads PEM file, returns RSA private key object via `cryptography.hazmat.primitives.serialization.load_pem_private_key`.
       - `build_kalshi_headers(method: str, path: str, private_key, api_key_id: str) -> dict` — per RESEARCH Pattern 1. Strips query params from path. Signs `timestamp_ms + METHOD + clean_path`. Returns dict with three KALSHI-ACCESS-* headers.
    3. Create `src/pmtb/kalshi/errors.py`:
       - `KalshiAPIError(Exception)` — base class with status_code, message.
       - `KalshiRateLimitError(KalshiAPIError)` — 429 responses.
       - `KalshiServerError(KalshiAPIError)` — 5xx responses.
       - `KalshiClientError(KalshiAPIError)` — 4xx (non-429) responses.
       - `classify_error(status_code: int, message: str) -> KalshiAPIError` — returns the appropriate error type.
       - `kalshi_retry` — tenacity decorator: `@retry(wait=wait_exponential_jitter(initial=1, max=30, jitter=3), stop=stop_after_attempt(5), retry=retry_if_exception_type((KalshiRateLimitError, KalshiServerError)), reraise=True)`.
    4. Write tests using a generated test RSA key (generate in conftest or fixture via `cryptography.hazmat.primitives.asymmetric.rsa.generate_private_key`). For retry tests, mock the retried function to raise errors then succeed.
    5. Run tests — RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/kalshi/test_auth.py tests/kalshi/test_retry.py -x -q</automated>
  </verify>
  <done>build_kalshi_headers generates valid RSA-PSS signed headers. Error classification maps status codes correctly. Retry decorator retries 429/5xx with exponential backoff, raises 4xx immediately.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KalshiClient wrapping SDK with authenticated methods</name>
  <files>
    src/pmtb/kalshi/client.py
  </files>
  <behavior>
    - Test 1: KalshiClient constructs with Settings and loads private key
    - Test 2: get_markets calls SDK and returns list of market dicts
    - Test 3: get_balance calls SDK and returns balance dict
    - Test 4: get_positions calls portfolio positions endpoint
    - Test 5: get_orders calls portfolio orders endpoint
    - Test 6: place_order sends order request with correct parameters
    - Test 7: cancel_order sends cancel request for given order_id
    - Test 8: Client uses demo URL when trading_mode is "paper"
    - Test 9: Client uses production URL when trading_mode is "live"
  </behavior>
  <action>
    1. Create `src/pmtb/kalshi/client.py`:
       - `KalshiClient` class:
         - Constructor takes `Settings`, calls `load_private_key(settings.kalshi_private_key_path)`, stores api_key_id. Selects base_url based on trading_mode (demo vs production).
         - `_headers(method, path)` — calls `build_kalshi_headers` with fresh timestamp every time (NEVER cache headers per anti-pattern).
         - Uses `kalshi-python-async` SDK internally. Initialize the SDK client with the base URL and auth headers. The exact SDK initialization may vary — inspect the `kalshi-python-async` package to determine how to pass auth headers (may need custom header injection or override).
         - If the SDK does not support custom header injection cleanly, use `httpx.AsyncClient` as fallback for REST calls with manual header signing.
         - Methods (all decorated with `@kalshi_retry`):
           - `async get_markets(**kwargs) -> list` — GET /trade-api/v2/markets
           - `async get_market(ticker: str) -> dict` — GET /trade-api/v2/markets/{ticker}
           - `async get_balance() -> dict` — GET /trade-api/v2/portfolio/balance
           - `async get_positions() -> list` — GET /trade-api/v2/portfolio/positions
           - `async get_orders(status: str | None = None) -> list` — GET /trade-api/v2/portfolio/orders
           - `async place_order(market_ticker: str, side: str, quantity: int, price: int, order_type: str = "limit") -> dict` — POST /trade-api/v2/portfolio/orders
           - `async cancel_order(order_id: str) -> dict` — DELETE /trade-api/v2/portfolio/orders/{order_id}
         - Each method: builds fresh headers, makes request, classifies HTTP errors via `classify_error`, returns parsed JSON.
         - Bind loguru logger: `logger.bind(endpoint=path)` for each call.
         - Increment `API_CALLS` Prometheus counter on each request (labels: endpoint, status).
    2. Write tests using mock httpx/SDK responses. No real API calls in unit tests.
    3. Run tests — RED then GREEN.

    NOTE: Inspect `kalshi-python-async` 3.8.0 to understand its client interface. If the SDK wraps HTTP calls internally and does not expose a way to inject custom headers, implement REST calls directly with `httpx.AsyncClient` and manual header signing. Add httpx to dependencies if needed (`uv add httpx`).
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/kalshi/ -x -q</automated>
  </verify>
  <done>KalshiClient makes authenticated REST calls with fresh RSA-PSS headers per request. All 7 REST methods work with retry on 429/5xx. Demo vs production URL selected by trading_mode. API call metrics tracked.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kalshi/ -x -q` passes all auth, retry, and client tests
- `uv run python -c "from pmtb.kalshi.client import KalshiClient; from pmtb.kalshi.auth import build_kalshi_headers; from pmtb.kalshi.errors import kalshi_retry; print('All imports OK')"` succeeds
</verification>

<success_criteria>
RSA-PSS auth headers generated correctly. Error classification maps 429 -> retry, 5xx -> retry, 4xx -> raise. KalshiClient wraps all needed REST endpoints with retry and metrics. Demo/production URL routing works via config.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md`
</output>
