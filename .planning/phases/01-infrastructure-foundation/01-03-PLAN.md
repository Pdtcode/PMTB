---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pmtb/paper.py
  - src/pmtb/executor.py
  - tests/test_paper.py
autonomous: true
requirements:
  - INFR-08

must_haves:
  truths:
    - "Paper trading mode routes all order calls to a no-op handler that returns simulated responses"
    - "Live trading mode routes order calls to the real KalshiClient"
    - "Both PaperOrderExecutor and KalshiOrderExecutor implement the same OrderExecutorProtocol"
    - "Paper mode is toggled via TRADING_MODE config without code changes"
    - "Simulated orders get unique paper-* IDs and are logged"
  artifacts:
    - path: "src/pmtb/executor.py"
      provides: "OrderExecutorProtocol and factory function"
      exports: ["OrderExecutorProtocol", "create_executor"]
    - path: "src/pmtb/paper.py"
      provides: "PaperOrderExecutor no-op implementation"
      exports: ["PaperOrderExecutor"]
  key_links:
    - from: "src/pmtb/executor.py"
      to: "src/pmtb/config.py"
      via: "create_executor reads trading_mode from Settings"
      pattern: "trading_mode.*paper"
    - from: "src/pmtb/paper.py"
      to: "src/pmtb/executor.py"
      via: "Implements OrderExecutorProtocol"
      pattern: "OrderExecutorProtocol"
---

<objective>
Paper trading mode with protocol-based executor injection.

Purpose: Paper trading mode (INFR-08) lets the full pipeline run without placing real orders. The executor protocol defines the interface that all downstream phases use — they never import KalshiClient directly for order placement.
Output: OrderExecutorProtocol, PaperOrderExecutor, and factory that selects executor based on config.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderExecutorProtocol and PaperOrderExecutor</name>
  <files>
    src/pmtb/executor.py, src/pmtb/paper.py,
    tests/test_paper.py
  </files>
  <behavior>
    - Test 1: PaperOrderExecutor.place_order returns dict with order_id starting with "paper-" and status "simulated"
    - Test 2: PaperOrderExecutor.cancel_order returns dict with status "cancelled"
    - Test 3: PaperOrderExecutor.get_positions returns empty list
    - Test 4: PaperOrderExecutor.get_orders returns list of previously placed paper orders
    - Test 5: PaperOrderExecutor logs each simulated order via loguru
    - Test 6: PaperOrderExecutor stores simulated orders in-memory for later inspection
    - Test 7: create_executor("paper", ...) returns PaperOrderExecutor
    - Test 8: create_executor("live", ...) returns an executor that delegates to KalshiClient
  </behavior>
  <action>
    1. Create `src/pmtb/executor.py`:
       - `OrderExecutorProtocol(Protocol)` with methods per RESEARCH Pattern 7:
         - `async place_order(market_ticker: str, side: str, quantity: int, price: int, order_type: str = "limit") -> dict`
         - `async cancel_order(order_id: str) -> dict`
         - `async get_positions() -> list`
         - `async get_orders(status: str | None = None) -> list`
       - `LiveOrderExecutor` class that delegates all methods to a `KalshiClient` instance. Accepts KalshiClient in constructor.
       - `create_executor(settings: Settings, kalshi_client: KalshiClient | None = None) -> OrderExecutorProtocol`:
         - If trading_mode == "paper": return PaperOrderExecutor()
         - If trading_mode == "live": return LiveOrderExecutor(kalshi_client) — raises ValueError if kalshi_client is None
    2. Create `src/pmtb/paper.py`:
       - `PaperOrderExecutor` class implementing OrderExecutorProtocol:
         - Internal `_orders: list[dict]` for in-memory tracking of simulated orders.
         - `place_order`: generates UUID paper-{uuid4()} order_id, creates order dict with all params + status "simulated" + timestamp, appends to _orders, logs via loguru `logger.bind(paper_mode=True).info("Simulated order placed", ...)`, returns the order dict.
         - `cancel_order`: finds order in _orders by order_id, updates status to "cancelled", returns status dict. If not found, returns {"status": "not_found"}.
         - `get_positions`: returns empty list (paper mode has no real positions).
         - `get_orders(status)`: returns _orders filtered by status if provided, else all.
    3. Write tests. For create_executor("live") test, mock KalshiClient.
    4. Run tests — RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/test_paper.py -x -q</automated>
  </verify>
  <done>PaperOrderExecutor simulates all order operations with in-memory tracking and logging. LiveOrderExecutor delegates to KalshiClient. Factory selects correct executor based on TRADING_MODE config. Protocol ensures both implementations are interchangeable.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_paper.py -x -q` passes
- `uv run python -c "from pmtb.executor import OrderExecutorProtocol, create_executor; from pmtb.paper import PaperOrderExecutor; print('OK')"` succeeds
</verification>

<success_criteria>
Paper trading mode fully simulates order placement, cancellation, and listing. Protocol-based injection means downstream code never needs to know if it is paper or live. Factory selects executor based on config.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md`
</output>
