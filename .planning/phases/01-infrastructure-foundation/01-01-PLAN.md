---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .env.example
  - .gitignore
  - config.yaml
  - src/pmtb/__init__.py
  - src/pmtb/config.py
  - src/pmtb/logging_.py
  - src/pmtb/metrics.py
  - src/pmtb/db/__init__.py
  - src/pmtb/db/engine.py
  - src/pmtb/db/models.py
  - src/pmtb/db/session.py
  - migrations/env.py
  - migrations/script.py.mako
  - alembic.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/db/__init__.py
  - tests/db/test_session.py
  - tests/db/test_migrations.py
autonomous: true
requirements:
  - INFR-03
  - INFR-04
  - INFR-05

must_haves:
  truths:
    - "Settings class loads values from .env and config.yaml with correct precedence (env vars > .env > YAML > defaults)"
    - "Settings validation rejects missing required fields at startup with a clear error"
    - "Async DB session connects to PostgreSQL and can execute a simple query"
    - "Alembic migrations run from scratch and produce all tables (markets, orders, positions, trades, signals, model_outputs, metrics)"
    - "Structured JSON logs are emitted to stdout with loguru serialize=True"
  artifacts:
    - path: "src/pmtb/config.py"
      provides: "Pydantic Settings class with YAML + .env sources"
      exports: ["Settings"]
    - path: "src/pmtb/db/engine.py"
      provides: "Async engine and session factory"
      exports: ["engine", "AsyncSessionLocal"]
    - path: "src/pmtb/db/models.py"
      provides: "SQLAlchemy ORM models for all core tables"
      contains: "class Market"
    - path: "src/pmtb/db/session.py"
      provides: "Session context manager"
      exports: ["get_session"]
    - path: "src/pmtb/logging_.py"
      provides: "Loguru configuration with JSON + rotating file"
      exports: ["configure_logging"]
    - path: "src/pmtb/metrics.py"
      provides: "Prometheus metrics registry"
      exports: ["CYCLE_COUNT", "CYCLE_LATENCY"]
    - path: "migrations/env.py"
      provides: "Alembic async env.py"
  key_links:
    - from: "src/pmtb/db/engine.py"
      to: "src/pmtb/config.py"
      via: "Settings.database_url passed to create_async_engine"
      pattern: "create_async_engine.*database_url"
    - from: "migrations/env.py"
      to: "src/pmtb/db/models.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata"
    - from: "src/pmtb/db/models.py"
      to: "sqlalchemy"
      via: "DeclarativeBase with naming_convention"
      pattern: "naming_convention"
---

<objective>
Project scaffolding, configuration management, database layer, and observability foundation.

Purpose: Every downstream plan and phase imports config, DB sessions, logger, and metrics from this plan. Nothing else can run without these.
Output: Working Python project with uv, pydantic-settings config, SQLAlchemy async DB layer, Alembic migrations, loguru logging, and Prometheus metrics.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding and configuration management</name>
  <files>
    pyproject.toml, .python-version, .env.example, .gitignore, config.yaml,
    src/pmtb/__init__.py, src/pmtb/config.py,
    tests/__init__.py, tests/conftest.py, tests/test_config.py
  </files>
  <behavior>
    - Test 1: Settings loads database_url from .env file
    - Test 2: Settings loads edge_threshold from config.yaml
    - Test 3: Environment variable overrides .env value
    - Test 4: TRADING_MODE defaults to "paper" and rejects invalid values
    - Test 5: Missing required field (database_url) raises ValidationError at startup
    - Test 6: CLI --paper flag concept: TRADING_MODE=paper is valid
  </behavior>
  <action>
    1. Create `.python-version` with `3.13`.
    2. Create `pyproject.toml` with project metadata, dependencies (kalshi-python-async, websockets, sqlalchemy[asyncio], asyncpg, alembic, pydantic-settings[yaml], loguru, tenacity, prometheus_client, cryptography, pyyaml), dev dependencies (pytest, pytest-asyncio, pytest-mock), and pytest config (`asyncio_mode = "auto"`, markers = ["demo: requires demo API credentials"]).
    3. Run `uv sync` to install all dependencies and create virtualenv.
    4. Create `.gitignore` with Python defaults, `.env`, `logs/`, `__pycache__/`, `.venv/`.
    5. Create `.env.example` with all env var names (DATABASE_URL, KALSHI_API_KEY_ID, KALSHI_PRIVATE_KEY_PATH, TRADING_MODE, LOG_LEVEL) — no real values, just placeholders.
    6. Create `config.yaml` with non-secret config defaults: edge_threshold: 0.04, kelly_alpha: 0.25, max_drawdown: 0.08, scan_interval_seconds: 300, rate_limit_per_second: 10.
    7. Create `src/pmtb/__init__.py` (empty).
    8. Create `src/pmtb/config.py` with Settings class per RESEARCH Pattern 5:
       - Extends BaseSettings with SettingsConfigDict(env_file=".env", yaml_file="config.yaml")
       - Fields: trading_mode (default "paper", pattern "^(paper|live)$"), database_url (required), kalshi_api_key_id (required), kalshi_private_key_path (required), edge_threshold (0.04), kelly_alpha (0.25), max_drawdown (0.08), scan_interval_seconds (300), rate_limit_per_second (10), log_level ("INFO"), kalshi_base_url (default "https://api.elections.kalshi.com"), kalshi_ws_url (default "wss://api.elections.kalshi.com/trade-api/ws/v2"), kalshi_demo_base_url (default "https://demo-api.kalshi.co"), kalshi_demo_ws_url (default "wss://demo-api.kalshi.co/trade-api/ws/v2")
       - Override settings_customise_sources: env_settings > YamlConfigSettingsSource > dotenv_settings > init_settings
    9. Write tests in tests/test_config.py covering all behaviors above. Use tmp_path for temp .env and config.yaml files, monkeypatch for env vars.
    10. Run tests — RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>Settings class loads from .env + YAML + env vars with correct precedence. Missing required fields raise ValidationError. TRADING_MODE validates against paper|live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database layer with SQLAlchemy async + Alembic migrations</name>
  <files>
    src/pmtb/db/__init__.py, src/pmtb/db/engine.py, src/pmtb/db/models.py, src/pmtb/db/session.py,
    alembic.ini, migrations/env.py, migrations/script.py.mako,
    tests/db/__init__.py, tests/db/test_session.py, tests/db/test_migrations.py
  </files>
  <behavior>
    - Test 1: AsyncSessionLocal produces a working async session that can execute `SELECT 1`
    - Test 2: get_session context manager yields a session and closes it
    - Test 3: All ORM models (Market, Order, Position, Trade, Signal, ModelOutput, PerformanceMetric) have expected columns
    - Test 4: Alembic migration generates tables matching model metadata
  </behavior>
  <action>
    1. Create `src/pmtb/db/__init__.py` (empty).
    2. Create `src/pmtb/db/models.py`:
       - Define NAMING_CONVENTION per RESEARCH Pattern 4.
       - Create Base = DeclarativeBase with MetaData(naming_convention=NAMING_CONVENTION).
       - All timestamps use `DateTime(timezone=True)` with `datetime.now(datetime.UTC)` — NEVER `datetime.utcnow()`.
       - Soft delete via `status` fields (not hard delete) per user decision.
       - Models:
         - **Market**: id (UUID PK), ticker (str unique), title (str), category (str), status (str, default "active"), close_time (DateTime tz), created_at, updated_at. Indexes on ticker, status, close_time.
         - **Order**: id (UUID PK), market_id (FK Market), side (str "yes"/"no"), quantity (int), price (Numeric), order_type (str), status (str default "pending"), kalshi_order_id (str nullable unique), fill_price (Numeric nullable), filled_quantity (int default 0), placed_at (DateTime tz), updated_at. Indexes on market_id, status, kalshi_order_id.
         - **Position**: id (UUID PK), market_id (FK Market unique), side (str), quantity (int), avg_price (Numeric), current_value (Numeric nullable), status (str default "open"), opened_at (DateTime tz), closed_at (DateTime tz nullable), updated_at.
         - **Trade**: id (UUID PK), order_id (FK Order), market_id (FK Market), side (str), quantity (int), price (Numeric), pnl (Numeric nullable), resolved_outcome (str nullable), resolved_at (DateTime tz nullable), created_at.
         - **Signal**: id (UUID PK), market_id (FK Market), source (str), sentiment (str "bullish"/"bearish"/"neutral"), confidence (Numeric), raw_data (JSON nullable), cycle_id (str), created_at. Index on (market_id, source, created_at).
         - **ModelOutput**: id (UUID PK), market_id (FK Market), p_model (Numeric), p_market (Numeric nullable), confidence_low (Numeric), confidence_high (Numeric), signal_weights (JSON nullable), model_version (str), used_llm (bool default false), cycle_id (str), created_at. Index on (market_id, created_at).
         - **PerformanceMetric**: id (UUID PK), metric_name (str), metric_value (Numeric), period (str nullable), computed_at (DateTime tz). Index on (metric_name, computed_at).
    3. Create `src/pmtb/db/engine.py` per RESEARCH Pattern 6:
       - `create_engine_from_settings(settings: Settings)` returns async engine with pool_size=5, max_overflow=10, pool_pre_ping=True, pool_recycle=300.
       - `create_session_factory(engine)` returns async_sessionmaker.
    4. Create `src/pmtb/db/session.py`:
       - `get_session()` async context manager yielding AsyncSession from the factory.
    5. Run `uv run alembic init --template async migrations` to scaffold Alembic.
       - Edit `alembic.ini` to set sqlalchemy.url placeholder.
       - Edit `migrations/env.py` to import Base.metadata as target_metadata, read database_url from Settings, use async engine with `run_sync` bridge per RESEARCH Pattern 4.
    6. Generate initial migration: `uv run alembic revision --autogenerate -m "initial_schema"`.
    7. Write tests in tests/db/. Tests should use a test PostgreSQL database (conftest.py fixture that creates a test DB, runs migrations, tears down). If no test PostgreSQL is available, tests should be marked with `@pytest.mark.skipif` and a clear message. Also write model introspection tests that verify column existence without a live DB.
    8. Run tests — RED then GREEN.

    NOTE: The test DB fixture in conftest.py should create a `pmtb_test` database. The DATABASE_URL for tests should be configurable via TEST_DATABASE_URL env var, defaulting to `postgresql+asyncpg://localhost:5432/pmtb_test`.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/db/ tests/test_config.py -x -q</automated>
  </verify>
  <done>ORM models define all 7 tables with correct columns, types, indexes, and foreign keys. Alembic async env.py generates and runs migrations. Session factory produces working async sessions. All timestamps are timezone-aware UTC.</done>
</task>

<task type="auto">
  <name>Task 3: Structured logging and Prometheus metrics</name>
  <files>
    src/pmtb/logging_.py, src/pmtb/metrics.py
  </files>
  <action>
    1. Create `src/pmtb/logging_.py` per RESEARCH code example:
       - `configure_logging(settings: Settings)`: removes default loguru handler, adds JSON stdout sink (serialize=True, level from settings.log_level), adds rotating file sink ("logs/pmtb_{time:YYYY-MM-DD}.log", rotation="100 MB", retention="7 days", level="DEBUG", human-readable format).
       - Export `logger` from loguru for use with `logger.bind(cycle_id=..., trade_id=...)` pattern.
    2. Create `src/pmtb/metrics.py`:
       - Use `prometheus_client` library.
       - Disable default process collectors for macOS compatibility: unregister PROCESS_COLLECTOR and PLATFORM_COLLECTOR if they exist.
       - Define metrics: CYCLE_COUNT (Counter, "pmtb_scan_cycles_total"), CYCLE_LATENCY (Histogram, "pmtb_scan_cycle_duration_seconds"), ERROR_COUNT (Counter, "pmtb_errors_total", labels=["error_type"]), OPEN_POSITIONS (Gauge, "pmtb_open_positions"), API_CALLS (Counter, "pmtb_api_calls_total", labels=["endpoint", "status"]).
       - `start_metrics_server(port: int = 9090)`: starts prometheus_client HTTP server in a thread.
    3. Verify logging works by running a quick script that imports configure_logging, configures it, and emits a log line.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run python -c "from pmtb.logging_ import configure_logging; from pmtb.metrics import CYCLE_COUNT, start_metrics_server; print('OK')"</automated>
  </verify>
  <done>Loguru configured with JSON stdout + rotating file. Prometheus metrics defined and importable. Metrics server starts without error. No /proc dependency on macOS.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` passes all config and DB tests
- `uv run python -c "from pmtb.config import Settings; from pmtb.db.models import Base; from pmtb.logging_ import configure_logging; from pmtb.metrics import CYCLE_COUNT; print('All imports OK')"` succeeds
- `uv run alembic upgrade head` runs without error against a local PostgreSQL
- config.yaml + .env.example exist with documented fields
</verification>

<success_criteria>
Settings class validates and loads from YAML + .env + env vars. Seven ORM models with full schema. Alembic async migrations work from scratch. Loguru JSON logging configured. Prometheus metrics defined. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
