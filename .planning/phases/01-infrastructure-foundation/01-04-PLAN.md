---
phase: 01-infrastructure-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/pmtb/kalshi/ws_client.py
  - src/pmtb/reconciler.py
  - src/pmtb/main.py
  - tests/kalshi/test_ws_reconnect.py
  - tests/test_reconciler.py
autonomous: true
requirements:
  - INFR-02
  - INFR-07

must_haves:
  truths:
    - "WebSocket connects to Kalshi WSS endpoint with signed headers and receives subscription confirmation"
    - "WebSocket reconnects automatically after disconnect with fixed 5-second retry per user decision"
    - "WebSocket subscribes to orderbook_delta and fill channels"
    - "Position reconciler compares Kalshi API positions/orders with DB state on startup"
    - "Reconciler detects orphaned orders (in DB but not on Kalshi) and updates their status"
    - "Reconciler detects missing positions (on Kalshi but not in DB) and inserts them"
  artifacts:
    - path: "src/pmtb/kalshi/ws_client.py"
      provides: "WebSocket client with auto-reconnect"
      exports: ["KalshiWSClient", "run_ws_client"]
    - path: "src/pmtb/reconciler.py"
      provides: "Position reconciliation on startup"
      exports: ["reconcile_positions"]
    - path: "src/pmtb/main.py"
      provides: "Application entry point with startup/shutdown lifecycle"
      exports: ["main"]
  key_links:
    - from: "src/pmtb/kalshi/ws_client.py"
      to: "src/pmtb/kalshi/auth.py"
      via: "build_kalshi_headers for WebSocket handshake"
      pattern: "build_kalshi_headers.*GET.*ws"
    - from: "src/pmtb/kalshi/ws_client.py"
      to: "websockets"
      via: "websockets.connect with additional_headers"
      pattern: "websockets\\.connect"
    - from: "src/pmtb/reconciler.py"
      to: "src/pmtb/kalshi/client.py"
      via: "KalshiClient.get_positions and get_orders"
      pattern: "get_positions|get_orders"
    - from: "src/pmtb/reconciler.py"
      to: "src/pmtb/db/session.py"
      via: "get_session for DB queries"
      pattern: "get_session"
    - from: "src/pmtb/main.py"
      to: "src/pmtb/reconciler.py"
      via: "reconcile_positions called at startup"
      pattern: "reconcile_positions"
---

<objective>
Kalshi WebSocket client for real-time data and position reconciliation on startup.

Purpose: WebSocket (INFR-02) provides real-time orderbook and fill events needed by Phase 5 risk management and Phase 6 execution tracking. Position reconciliation (INFR-07) prevents orphaned orders after crashes/restarts. The main.py entry point wires everything together.
Output: Working WebSocket client with auto-reconnect, position reconciler, and application entry point.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebSocket client with auto-reconnect</name>
  <files>
    src/pmtb/kalshi/ws_client.py,
    tests/kalshi/test_ws_reconnect.py
  </files>
  <behavior>
    - Test 1: KalshiWSClient connects with signed headers (mock websockets.connect)
    - Test 2: On successful connect, sends subscription message for orderbook_delta and fill channels
    - Test 3: Incoming messages are passed to the on_message callback as parsed dicts
    - Test 4: On ConnectionClosed exception, waits 5 seconds then reconnects (mock asyncio.sleep)
    - Test 5: On OSError, waits 5 seconds then reconnects
    - Test 6: Subscription message includes market_ticker parameter
    - Test 7: WS URL selected based on trading_mode (demo vs production)
  </behavior>
  <action>
    1. Create `src/pmtb/kalshi/ws_client.py` per RESEARCH Pattern 3:
       - `KalshiWSClient` class:
         - Constructor takes `Settings`, loads private key, stores config.
         - `_ws_url` property: returns demo WS URL if trading_mode == "paper", production otherwise.
         - `async subscribe(ws, channels: list[str], market_tickers: list[str])`: sends JSON subscription command `{"id": 1, "cmd": "subscribe", "params": {"channels": channels, "market_tickers": market_tickers}}`.
         - `async unsubscribe(ws, channels: list[str], market_tickers: list[str])`: sends JSON unsubscribe command.
         - `async run(on_message: Callable, channels: list[str] = ["orderbook_delta", "fill"], market_tickers: list[str] | None = None)`:
           - `while True:` loop wrapping `async with websockets.connect(self._ws_url, additional_headers=headers) as ws:`.
           - Headers generated fresh for each connection attempt via `build_kalshi_headers("GET", "/trade-api/ws/v2", ...)`.
           - After connect, subscribes to specified channels.
           - `async for message in ws:` passes parsed JSON to on_message callback.
           - `except (websockets.ConnectionClosed, OSError) as e:` logs the error and `await asyncio.sleep(5)` — fixed 5-second retry per user decision (NOT exponential).
           - Logs reconnection events via loguru.
       - `run_ws_client(settings, on_message, **kwargs)` convenience function that creates KalshiWSClient and calls run().
    2. Write tests mocking `websockets.connect` as an async context manager. Use `AsyncMock` for the websocket object. For reconnect tests, make the first connection raise ConnectionClosed, verify asyncio.sleep(5) is called, then succeed on second attempt.
    3. Run tests — RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/kalshi/test_ws_reconnect.py -x -q</automated>
  </verify>
  <done>WebSocket client connects with RSA-PSS signed headers, subscribes to channels, routes messages to callback, and auto-reconnects with fixed 5-second delay on any connection error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Position reconciler and application entry point</name>
  <files>
    src/pmtb/reconciler.py, src/pmtb/main.py,
    tests/test_reconciler.py
  </files>
  <behavior>
    - Test 1: reconcile_positions detects order in DB with status "pending" that does not exist on Kalshi API — marks as "orphaned"
    - Test 2: reconcile_positions detects position on Kalshi API that is not in DB — inserts it
    - Test 3: reconcile_positions detects order on Kalshi with different status than DB — updates DB status
    - Test 4: reconcile_positions with no discrepancies — no changes made, logs "positions reconciled: 0 discrepancies"
    - Test 5: reconcile_positions logs each discrepancy found with details
  </behavior>
  <action>
    1. Create `src/pmtb/reconciler.py`:
       - `async reconcile_positions(kalshi_client: KalshiClient, session_factory) -> ReconciliationResult`:
         - Fetches all open orders and positions from Kalshi API via kalshi_client.
         - Fetches all non-terminal orders (status not in ["filled", "cancelled", "expired"]) and open positions from DB.
         - Compares:
           - Orders in DB not found on Kalshi: mark status = "orphaned" in DB, log warning.
           - Orders on Kalshi not in DB: insert into DB with current status, log info.
           - Orders with mismatched status: update DB to match Kalshi, log info.
           - Positions on Kalshi not in DB: insert into DB, log info.
           - Positions in DB not on Kalshi: mark status = "closed" in DB, log warning.
         - Returns `ReconciliationResult` (dataclass with counts: orphaned_orders, new_orders, updated_orders, new_positions, closed_positions).
         - Logs summary: "Position reconciliation complete: {result}".
    2. Create `src/pmtb/main.py`:
       - `async main()`:
         - Load Settings (fail fast on validation errors).
         - Configure logging via `configure_logging(settings)`.
         - Log startup info: version, trading_mode, database_url (masked).
         - Create async engine and session factory from settings.
         - Create KalshiClient from settings.
         - Create executor via `create_executor(settings, kalshi_client)`.
         - Run `reconcile_positions(kalshi_client, session_factory)` — logs results, does not fail startup on reconciliation errors (logs warning and continues).
         - Start metrics server on configured port.
         - Log "PMTB started in {trading_mode} mode".
         - (Placeholder for scan loop — Phase 2 will add the actual loop.)
         - Await keyboard interrupt or shutdown signal for clean exit.
       - `if __name__ == "__main__": asyncio.run(main())`
    3. Write tests for reconciler using mocked KalshiClient and mocked DB session. Create fixtures that return pre-defined API responses and DB state, then assert reconciliation actions.
    4. Run tests — RED then GREEN.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/test_reconciler.py -x -q</automated>
  </verify>
  <done>Position reconciler detects and resolves all discrepancies between Kalshi API state and DB state on startup. main.py wires all components together and starts cleanly in paper mode. Orphaned orders are marked, missing positions are inserted.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/kalshi/test_ws_reconnect.py tests/test_reconciler.py -x -q` passes
- `uv run python -c "from pmtb.kalshi.ws_client import KalshiWSClient; from pmtb.reconciler import reconcile_positions; from pmtb.main import main; print('OK')"` succeeds
- Full test suite: `uv run pytest tests/ -x -q --ignore=tests/kalshi/test_client_integration.py --ignore=tests/kalshi/test_ws_client.py` passes
</verification>

<success_criteria>
WebSocket client connects with signed headers, subscribes to channels, and auto-reconnects on disconnect with 5-second fixed retry. Position reconciler detects orphaned orders and missing positions on startup. main.py wires config, DB, Kalshi client, executor, reconciler, logging, and metrics into a working application entry point. All unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md`
</output>
