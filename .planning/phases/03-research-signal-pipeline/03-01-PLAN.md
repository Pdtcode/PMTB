---
phase: 03-research-signal-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pmtb/research/__init__.py
  - src/pmtb/research/agent.py
  - src/pmtb/research/models.py
  - src/pmtb/config.py
  - config.yaml
  - tests/research/__init__.py
  - tests/research/test_models.py
autonomous: true
requirements:
  - RSRCH-07

must_haves:
  truths:
    - "ResearchAgent Protocol is importable and runtime_checkable"
    - "SignalBundle.to_features() returns flat numeric dict with NaN for missing sources"
    - "SourceSummary, SourceResult, SignalClassification models validate correctly"
    - "Research config fields load from YAML/env following Settings pattern"
  artifacts:
    - path: "src/pmtb/research/agent.py"
      provides: "ResearchAgent Protocol with source_name and fetch()"
      contains: "@runtime_checkable"
    - path: "src/pmtb/research/models.py"
      provides: "SignalBundle, SourceSummary, SourceResult, SignalClassification"
      exports: ["SignalBundle", "SourceSummary", "SourceResult", "SignalClassification"]
    - path: "src/pmtb/config.py"
      provides: "Research config fields added to Settings"
      contains: "research_agent_timeout"
    - path: "tests/research/test_models.py"
      provides: "Unit tests for models and to_features()"
  key_links:
    - from: "src/pmtb/research/models.py"
      to: "src/pmtb/scanner/models.py"
      via: "SignalBundle.ticker matches MarketCandidate.ticker"
      pattern: "ticker.*str"
---

<objective>
Define the type contracts, Pydantic models, and configuration fields that all research agents and the pipeline orchestrator will build against.

Purpose: Establish the interfaces first so Plans 02-04 implement against concrete contracts, not guesswork. Also satisfies RSRCH-07 (topic classification uses MarketCandidate.category — no re-classification).
Output: ResearchAgent Protocol, SignalBundle/SourceSummary/SourceResult/SignalClassification models, research Settings fields, and unit tests for models.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-research-signal-pipeline/03-CONTEXT.md
@.planning/phases/03-research-signal-pipeline/03-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from existing codebase -->

From src/pmtb/scanner/models.py:
```python
class MarketCandidate(BaseModel):
    ticker: str
    title: str
    category: str
    event_context: dict
    close_time: datetime
    yes_bid: float = Field(ge=0.0, le=1.0)
    yes_ask: float = Field(ge=0.0, le=1.0)
    implied_probability: float = Field(ge=0.0, le=1.0)
    spread: float = Field(ge=0.0)
    volume_24h: float = Field(ge=0.0)
    volatility_score: float | None = None
```

From src/pmtb/db/models.py:
```python
class Signal(Base):
    __tablename__ = "signals"
    id: Mapped[uuid.UUID]
    market_id: Mapped[uuid.UUID]  # FK to markets.id
    source: Mapped[str]           # "reddit", "rss", "trends", "twitter"
    sentiment: Mapped[str]        # "bullish", "bearish", "neutral"
    confidence: Mapped[Decimal]   # 0.0-1.0
    raw_data: Mapped[dict | None] # JSON
    cycle_id: Mapped[str]
    created_at: Mapped[datetime]
```

From src/pmtb/config.py:
```python
class Settings(BaseSettings):
    # Existing fields... research fields will be ADDED here
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", yaml_file="config.yaml")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create research type contracts and Protocol</name>
  <files>src/pmtb/research/__init__.py, src/pmtb/research/agent.py, src/pmtb/research/models.py, tests/research/__init__.py, tests/research/test_models.py</files>
  <behavior>
    - SignalClassification(sentiment="bullish", confidence=0.8, reasoning=None) validates
    - SignalClassification with sentiment not in {"bullish","bearish","neutral"} raises ValidationError
    - SourceResult with empty signals list is valid (source returned no data)
    - SourceSummary with sentiment=None and confidence=None is valid (source failed)
    - SignalBundle.to_features() returns dict with 8 keys (reddit_sentiment, reddit_confidence, rss_sentiment, rss_confidence, trends_sentiment, trends_confidence, twitter_sentiment, twitter_confidence)
    - SignalBundle.to_features() returns NaN for missing (None) sources, not 0.0
    - SignalBundle.to_features() maps bullish=1.0, neutral=0.0, bearish=-1.0
    - ResearchAgent Protocol is runtime_checkable — isinstance check works on a conforming class
  </behavior>
  <action>
    Create `src/pmtb/research/__init__.py` with empty init.

    Create `src/pmtb/research/agent.py`:
    - `@runtime_checkable` Protocol `ResearchAgent` with:
      - `source_name: str` class attribute
      - `async def fetch(self, candidate: MarketCandidate, query: str) -> SourceResult` method
    - Follow the same pattern as `OrderExecutorProtocol` in Phase 1.

    Create `src/pmtb/research/models.py`:
    - `SignalClassification(BaseModel)`: sentiment (Literal["bullish","bearish","neutral"]), confidence (float, 0-1), reasoning (str | None)
    - `SourceResult(BaseModel)`: source (str), signals (list[SignalClassification]), raw_data (dict | None = None) — raw API response for debugging
    - `SourceSummary(BaseModel)`: sentiment (str | None), confidence (float | None), signal_count (int) — aggregated per-source summary
    - `SignalBundle(BaseModel)`: ticker (str), cycle_id (str), reddit/rss/trends/twitter (SourceSummary | None = None each), plus `to_features()` method that returns flat numeric dict with NaN for missing sources. Use the exact implementation from 03-RESEARCH.md code example.

    Create `tests/research/__init__.py` (empty).
    Create `tests/research/test_models.py` with tests per behavior block above.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run pytest tests/research/test_models.py -x -q</automated>
  </verify>
  <done>All model tests pass. ResearchAgent Protocol is importable and runtime_checkable. SignalBundle.to_features() returns correct 8-key dict with NaN for None sources.</done>
</task>

<task type="auto">
  <name>Task 2: Add research config fields to Settings</name>
  <files>src/pmtb/config.py, config.yaml</files>
  <action>
    Add the following fields to the `Settings` class in `src/pmtb/config.py`, in a new `# --- Research settings ---` section after the scanner settings block:

    - `research_agent_timeout: float = Field(default=30.0, description="Timeout in seconds per research agent")`
    - `research_concurrency: int = Field(default=4, description="Max concurrent research agent calls per market")`
    - `vader_escalation_threshold: float = Field(default=0.3, description="VADER compound score abs threshold below which text is escalated to Claude")`
    - `query_cache_ttl_seconds: int = Field(default=3600, description="TTL for cached search queries per ticker")`
    - `research_results_per_source: int = Field(default=10, description="Number of results to fetch per source per market")`
    - `reddit_client_id: str | None = Field(default=None, description="Reddit API OAuth client ID")`
    - `reddit_client_secret: str | None = Field(default=None, description="Reddit API OAuth client secret")`
    - `reddit_user_agent: str = Field(default="pmtb-research/1.0", description="Reddit API user agent string")`
    - `anthropic_api_key: str | None = Field(default=None, description="Anthropic API key for Claude sentiment escalation — None = VADER-only mode")`
    - `rss_feeds: dict[str, list[str]] = Field(default_factory=dict, description="RSS feed URLs by market category")`

    Update `config.yaml` with sensible defaults:
    - `research_agent_timeout: 30`
    - `vader_escalation_threshold: 0.3`
    - `query_cache_ttl_seconds: 3600`
    - `research_results_per_source: 10`
    - `rss_feeds` with category mappings:
      - politics: [AP Politics RSS, Reuters Politics RSS]
      - economics: [Reuters Business RSS, Bloomberg RSS]
      - finance: [Yahoo Finance RSS, MarketWatch RSS]
      - sports: [ESPN RSS]
      - weather: [Weather.gov RSS]
      - science: [Nature RSS, Science Daily RSS]
      - general: [AP Top Headlines, Reuters Top News]

    Do NOT add reddit_client_id, reddit_client_secret, or anthropic_api_key to config.yaml — these are secrets loaded from .env only.
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && uv run python -c "from pmtb.config import Settings; print('research fields importable')" && uv run pytest tests/ -x -q --ignore=tests/research/</automated>
  </verify>
  <done>Settings class loads with new research fields. Existing tests still pass. config.yaml has RSS feed defaults per category.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/research/test_models.py -x -q` passes
- `uv run pytest tests/ -x -q` passes (no regressions)
- `from pmtb.research.agent import ResearchAgent` works
- `from pmtb.research.models import SignalBundle, SourceSummary, SourceResult, SignalClassification` works
- `Settings()` loads with research config fields
</verification>

<success_criteria>
All type contracts defined, importable, and tested. Settings extended with research fields. Downstream plans (02, 03, 04) can import these types without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-signal-pipeline/03-01-SUMMARY.md`
</output>
