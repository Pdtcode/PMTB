---
phase: 07-performance-tracking-and-learning-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pmtb/performance/__init__.py
  - src/pmtb/performance/models.py
  - src/pmtb/performance/metrics.py
  - src/pmtb/db/models.py
  - src/pmtb/config.py
  - migrations/versions/005_add_loss_analysis_backtest_run.py
  - tests/performance/__init__.py
  - tests/performance/test_metrics.py
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]

must_haves:
  truths:
    - "Brier score is computed correctly from resolved predictions and outcomes"
    - "Sharpe ratio is annualized with sqrt(252) from daily PnL"
    - "Win rate and profit factor are computed from resolved trades"
    - "All metrics are persisted to PerformanceMetric DB table with both alltime and rolling window periods"
    - "Metrics with fewer than 10 resolved trades return None (not misleading values)"
    - "Daily full recomputation method exists to ensure consistency with incremental updates"
  artifacts:
    - path: "src/pmtb/performance/models.py"
      provides: "MetricsSnapshot Pydantic model, ErrorType enum, LossAnalysisResult model, BacktestResult model"
      exports: ["MetricsSnapshot", "ErrorType", "LossAnalysisResult", "BacktestResult"]
    - path: "src/pmtb/performance/metrics.py"
      provides: "MetricsService with compute_brier, compute_sharpe, compute_win_rate, compute_profit_factor, compute_all, persist, recompute_all_windows"
      exports: ["MetricsService"]
    - path: "src/pmtb/db/models.py"
      provides: "LossAnalysis and BacktestRun ORM models added"
      contains: "class LossAnalysis"
    - path: "migrations/versions/005_add_loss_analysis_backtest_run.py"
      provides: "Alembic migration for new tables"
    - path: "tests/performance/test_metrics.py"
      provides: "Unit tests for all metrics computations including recompute_all_windows"
  key_links:
    - from: "src/pmtb/performance/metrics.py"
      to: "src/pmtb/db/models.py"
      via: "async session factory queries Trade, ModelOutput, PerformanceMetric"
      pattern: "select.*Trade.*resolved_outcome"
    - from: "src/pmtb/performance/metrics.py"
      to: "sklearn.metrics.brier_score_loss"
      via: "import"
      pattern: "brier_score_loss"
---

<objective>
Create the performance module foundation: Pydantic type contracts for all Phase 7 outputs, new DB models (LossAnalysis, BacktestRun), Alembic migration, Settings fields, and the MetricsService that computes Brier score, Sharpe ratio, win rate, and profit factor on resolved trades with dual-window support (alltime + rolling).

Purpose: Establishes the type contracts and metrics computation layer that all subsequent plans depend on. Without accurate metrics, retraining triggers and loss classification cannot function.

Output: `src/pmtb/performance/` package with models.py and metrics.py, updated DB models, migration, config fields, and passing tests.
</objective>

<execution_context>
@/Users/petertrinh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/petertrinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-performance-tracking-and-learning-loop/07-CONTEXT.md
@.planning/phases/07-performance-tracking-and-learning-loop/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/pmtb/db/models.py:
```python
class Base(DeclarativeBase):
    metadata = MetaData(naming_convention=NAMING_CONVENTION)

class Trade(Base):
    __tablename__ = "trades"
    id: Mapped[uuid.UUID]
    order_id: Mapped[uuid.UUID]
    market_id: Mapped[uuid.UUID]
    side: Mapped[str]
    quantity: Mapped[int]
    price: Mapped[Decimal]
    pnl: Mapped[Decimal | None]
    resolved_outcome: Mapped[str | None]
    resolved_at: Mapped[datetime | None]
    created_at: Mapped[datetime]

class ModelOutput(Base):
    __tablename__ = "model_outputs"
    id: Mapped[uuid.UUID]
    market_id: Mapped[uuid.UUID]
    p_model: Mapped[Decimal]
    p_market: Mapped[Decimal | None]
    signal_weights: Mapped[dict | None]
    model_version: Mapped[str]
    used_llm: Mapped[bool]
    cycle_id: Mapped[str]
    created_at: Mapped[datetime]

class PerformanceMetric(Base):
    __tablename__ = "performance_metrics"
    id: Mapped[uuid.UUID]
    metric_name: Mapped[str]
    metric_value: Mapped[Decimal]
    period: Mapped[str | None]
    computed_at: Mapped[datetime]
```

From src/pmtb/config.py:
```python
class Settings(BaseSettings):
    # Existing fields omitted for brevity
    # Add new fields following the same Field() pattern
```

From migrations/versions/:
```
003_add_trading_state.py
004_add_is_paper_column.py
f63a30b29f5b_initial_schema.py
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type contracts, DB models, migration, and Settings fields</name>
  <files>
    src/pmtb/performance/__init__.py,
    src/pmtb/performance/models.py,
    src/pmtb/db/models.py,
    src/pmtb/config.py,
    migrations/versions/005_add_loss_analysis_backtest_run.py
  </files>
  <behavior>
    - MetricsSnapshot has fields: brier_score (float|None), sharpe_ratio (float|None), win_rate (float|None), profit_factor (float|None), trade_count (int), period (str), computed_at (datetime)
    - ErrorType enum has values: edge_decay, signal_error, llm_error, sizing_error, market_shock, unknown
    - LossAnalysisResult has fields: trade_id (UUID), error_type (ErrorType), reasoning (str|None), classified_by (str — "rules" or "claude")
    - BacktestResult has fields: start_date, end_date, trade_count (int), brier_score (float|None), sharpe_ratio (float|None), win_rate (float|None), profit_factor (float|None), parameters (dict)
    - LossAnalysis DB model has: id (UUID PK), trade_id (UUID FK->trades.id), error_type (String), reasoning (String nullable), classified_by (String), created_at (DateTime tz)
    - BacktestRun DB model has: id (UUID PK), run_at (DateTime tz), start_date (DateTime tz), end_date (DateTime tz), trade_count (Integer), brier_score (Numeric nullable), sharpe_ratio (Numeric nullable), win_rate (Numeric nullable), profit_factor (Numeric nullable), parameters (JSON), created_at (DateTime tz)
    - Settings gains: brier_degradation_threshold (float, default 0.05), retraining_schedule_hours (int, default 168), rolling_window_days (int, default 30), retraining_half_life_days (float, default 30.0), settlement_poll_interval_seconds (int, default 60)
  </behavior>
  <action>
    1. Create `src/pmtb/performance/__init__.py` (empty).
    2. Create `src/pmtb/performance/models.py` with:
       - `ErrorType(str, Enum)` with values: edge_decay, signal_error, llm_error, sizing_error, market_shock, unknown
       - `MetricsSnapshot(BaseModel)` with fields as specified in behavior
       - `LossAnalysisResult(BaseModel)` with fields as specified
       - `BacktestResult(BaseModel)` with fields as specified
    3. Add to `src/pmtb/db/models.py`:
       - `LossAnalysis(Base)` table "loss_analyses" with FK to trades.id, index on trade_id
       - `BacktestRun(Base)` table "backtest_runs" with index on run_at
       - Add `loss_analysis` relationship to Trade model (one-to-many)
    4. Add to `src/pmtb/config.py` Settings class:
       - `brier_degradation_threshold: float = Field(default=0.05, description="...")`
       - `retraining_schedule_hours: int = Field(default=168, description="...")`
       - `rolling_window_days: int = Field(default=30, description="...")`
       - `retraining_half_life_days: float = Field(default=30.0, description="...")`
       - `settlement_poll_interval_seconds: int = Field(default=60, description="...")`
    5. Create manual Alembic migration `migrations/versions/005_add_loss_analysis_backtest_run.py`:
       - Follow existing migration patterns (003, 004)
       - Create loss_analyses table with all columns and FK constraint
       - Create backtest_runs table with all columns
       - Add indexes: ix_loss_analyses_trade_id, ix_backtest_runs_run_at
       - down_revision = "004" (chain from existing head)
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && python -c "from pmtb.performance.models import MetricsSnapshot, ErrorType, LossAnalysisResult, BacktestResult; from pmtb.db.models import LossAnalysis, BacktestRun; print('imports OK')"</automated>
  </verify>
  <done>All Pydantic models, DB ORM models, migration file, and Settings fields exist and import without errors. ErrorType enum has 6 values. LossAnalysis has FK to trades.id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MetricsService with dual-window computation, persistence, and daily recompute</name>
  <files>
    src/pmtb/performance/metrics.py,
    tests/performance/__init__.py,
    tests/performance/test_metrics.py
  </files>
  <behavior>
    - compute_brier([0.9, 0.1], [1, 0]) returns brier_score_loss result (~0.01)
    - compute_brier with fewer than 10 trades returns None
    - compute_sharpe([100, -50, 200, -30, 150]) returns annualized value using mean/std * sqrt(252)
    - compute_sharpe with all-zero daily PnL returns NaN (zero std guard)
    - compute_win_rate(wins=7, total=10) returns 0.7
    - compute_profit_factor([100, 200], [-50, -30]) returns 300/80 = 3.75
    - compute_profit_factor with no losses returns float("inf")
    - compute_all queries resolved trades (WHERE pnl IS NOT NULL AND resolved_at IS NOT NULL), computes all 4 metrics, returns MetricsSnapshot
    - compute_all with period="30d" filters to rolling_window_days
    - persist_metrics writes MetricsSnapshot fields to PerformanceMetric rows
    - Minimum sample guard: returns None for any metric when trade_count < 10
    - recompute_all_windows computes and persists both alltime and 30d windows, deleting stale PerformanceMetric rows for those periods first
    - recompute_all_windows is idempotent — running it multiple times produces the same result
  </behavior>
  <action>
    1. Create `tests/performance/__init__.py` (empty).
    2. Create `tests/performance/test_metrics.py` with unit tests:
       - test_brier_score: known inputs -> sklearn.metrics.brier_score_loss output
       - test_brier_score_below_minimum: fewer than 10 -> None
       - test_sharpe_ratio: known daily PnL -> annualized with sqrt(252)
       - test_sharpe_ratio_zero_std: all zeros -> NaN
       - test_win_rate: wins/total ratio
       - test_profit_factor: gross_profit/gross_loss
       - test_profit_factor_no_losses: returns inf
       - test_compute_all_filters_resolved: only includes WHERE pnl IS NOT NULL AND resolved_at IS NOT NULL
       - test_compute_all_rolling_window: period="30d" filters by rolling_window_days
       - test_recompute_all_windows: verifies both alltime and 30d snapshots are computed and persisted, stale rows deleted first
    3. Create `src/pmtb/performance/metrics.py` with MetricsService class:
       - Constructor: `__init__(self, session_factory, settings)` — stores async session factory and Settings
       - `compute_brier(p_models: list[float], outcomes: list[int]) -> float | None` — uses sklearn.metrics.brier_score_loss, returns None if len < 10
       - `compute_sharpe(daily_pnl: list[float]) -> float` — annualized mean/std * sqrt(252), NaN guard on zero std
       - `compute_win_rate(wins: int, total: int) -> float | None` — wins/total, None if total < 10
       - `compute_profit_factor(pnl_values: list[float]) -> float | None` — gross_profit/gross_loss, inf if no losses, None if total < 10
       - `async compute_all(period: str = "alltime") -> MetricsSnapshot` — queries Trade table for resolved trades (pnl IS NOT NULL, resolved_at IS NOT NULL), filters by rolling_window_days if period="30d", joins ModelOutput for p_model by matching market_id + created_at <= trade.created_at, computes all 4 metrics, returns MetricsSnapshot
       - `async persist_metrics(snapshot: MetricsSnapshot) -> None` — writes each non-None metric to PerformanceMetric table with period tag
       - `async update_on_resolution() -> None` — computes both alltime and rolling, persists both (incremental trigger per user decision)
       - `async recompute_all_windows() -> None` — full daily recomputation for consistency (per user decision: dual trigger with incremental + daily full recompute). Deletes existing PerformanceMetric rows for periods "alltime" and "30d", then computes and persists fresh snapshots for both windows. This catches any missed incremental updates and ensures rolling window Brier values driving retraining decisions are accurate.
       - Add Prometheus counters: METRICS_COMPUTED counter, METRICS_COMPUTE_DURATION histogram
       - Use asyncio.Lock to prevent concurrent metric writes (anti-pattern from Research)
       - Log with loguru: sample count, metric values, period
  </action>
  <verify>
    <automated>cd /Users/petertrinh/Downloads/Computer-Projects/PMTB && python -m pytest tests/performance/test_metrics.py -x -q</automated>
  </verify>
  <done>MetricsService computes Brier score, Sharpe ratio, win rate, and profit factor correctly. All tests pass. Minimum sample guard returns None for < 10 trades. Dual window (alltime + rolling) works. recompute_all_windows provides daily full recomputation for consistency.</done>
</task>

</tasks>

<verification>
- `python -c "from pmtb.performance.models import MetricsSnapshot, ErrorType; print('OK')"` succeeds
- `python -c "from pmtb.performance.metrics import MetricsService; print('OK')"` succeeds
- `python -c "from pmtb.db.models import LossAnalysis, BacktestRun; print('OK')"` succeeds
- `pytest tests/performance/test_metrics.py -x -q` all pass
- Settings fields accessible: `python -c "from pmtb.config import Settings; print(Settings.model_fields.keys())"` includes new fields
</verification>

<success_criteria>
- Brier score computed via sklearn.metrics.brier_score_loss matches expected values
- Sharpe ratio annualized with sqrt(252), handles zero-std edge case
- Win rate and profit factor computed correctly with edge cases (no losses -> inf)
- All metrics return None when fewer than 10 resolved trades exist
- LossAnalysis and BacktestRun DB models exist with correct schema
- Migration file 005 creates both new tables
- Settings has all 5 new fields with correct defaults
- recompute_all_windows method exists and recomputes both alltime and 30d windows from scratch
</success_criteria>

<output>
After completion, create `.planning/phases/07-performance-tracking-and-learning-loop/07-01-SUMMARY.md`
</output>
